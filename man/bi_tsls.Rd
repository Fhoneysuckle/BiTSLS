% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bi_tsls.R
\name{bi_tsls}
\alias{bi_tsls}
\title{Bi-directional Two-Stage Least Squares Estimation Function}
\usage{
bi_tsls(
  data,
  x_var,
  y_var,
  z_var,
  w_var,
  covariates = NULL,
  standardize_vars = TRUE,
  R_w = 0,
  R_z = 0
)
}
\arguments{
\item{data}{A data frame containing the necessary variables.}

\item{x_var}{Name of the X variable (primary variable in one direction).}

\item{y_var}{Name of the Y variable (primary variable in the other direction).}

\item{z_var}{Name of the Z (negative control exposure variable).}

\item{w_var}{Name of the W (negative control outcome variable).}

\item{covariates}{Optional vector of covariate variable names. If NULL, all other variables in the data will be used as covariates.}

\item{standardize_vars}{Logical, whether to standardize variables before analysis. Default is TRUE.}

\item{R_w}{Sensitivity parameter R_w. Default is 0.}

\item{R_z}{Sensitivity parameter R_z. Default is 0.}
}
\value{
A named vector containing the estimated causal effects in both directions (beta_xy and beta_yx).
}
\description{
The main function for bi-directional Two-Stage Least Squares (Bi-TSLS) estimation.
It allows for the estimation of causal effects in both directions between variables.
}
\examples{
\dontrun{
data <- read.csv("your_data.csv")
result <- bi_tsls(data, "X_variable", "Y_variable", "Z_variable", "W_variable")
print(result)
}
}
